import React from'react';import{motion}from'motion/react';export default function AgentOrbit({nodes=[],active}){return <div className="agentOrbit"><div className="orbitCore"><span>OBJECTIVE</span></div>{nodes.map((n,i)=>{const a=active===n.id;return <motion.div key={n.id} className={'orbitNode '+(a?'active':'')} style={{'--i':i,'--count':nodes.length}} animate={{scale:a?1.08:1,opacity:a?1:.72}} transition={{duration:.35}}><span>{n.icon}</span><small>{n.name}</small></motion.div>})}</div>}