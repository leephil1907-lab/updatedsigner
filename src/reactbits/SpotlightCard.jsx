import{useRef}from'react';import'./SpotlightCard.css';export default function SpotlightCard({children,className='',spotlightColor='rgba(255,255,255,.2)'}){const r=useRef(null);return <div ref={r} onMouseMove={e=>{const b=r.current.getBoundingClientRect();r.current.style.setProperty('--mouse-x',`${e.clientX-b.left}px`);r.current.style.setProperty('--mouse-y',`${e.clientY-b.top}px`);r.current.style.setProperty('--spotlight-color',spotlightColor)}} className={`spotlight ${className}`}>{children}</div>}